name: ednet_core_default_app
version: 1.0.3
description: ednet core default app is used to interpret a EDNetCore model and provide a web app to navigate through the model starting by its entry points
homepage: https://github.com/dzenanr/ednet_core_default_app
documentation: http://goo.gl/Fd08zZ

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=3.3.10"

dependencies:
  test: '>=0.12.0 <2.0.0'

  ednet_core:
    path: ../../../ednet_cms/packages/ednet_core

  intl:  ^0.18.0
